/** Single viewport — pre / intermediate / post pipeline stages. */
export const LEAK_SCENE_STAGE_COUNT = 3
export const LEAK_SCENE_SPRITE_COUNT = LEAK_SCENE_STAGE_COUNT

/** Imperative renders after textures land — fills _batchersByInstructionSet. */
export const RENDER_FRAMES_AFTER_TEXTURE = 8
